/**
 * TemplateConstants holds all constants, such
 * as breakpoints, timeouts, etc.
 */

export const authenticated = document.documentElement.hasAttribute('data-authenticated-account');
export const debug = false;
export const mobileBreakpoint = 640;